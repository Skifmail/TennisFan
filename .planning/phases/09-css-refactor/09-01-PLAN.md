# Plan 09-01: Phase 9 — CSS рефакторинг (анализ)

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Провести структурный анализ `static/css/style.css` и подготовить карту содержимого CSS без внесения любых изменений в код стилей.

**Источники:** пользовательская постановка задачи, текущее состояние `static/css/style.css`.  
**Зависимости:** отсутствуют.

---

## Prerequisites

- Файл `static/css/style.css` существует и доступен для чтения.
- Анализ выполняется только в read-only режиме для CSS.
- Результат анализа оформляется отдельным документом `CSS-MAP.md`.

---

## Task 1: Проанализировать `static/css/style.css` и создать карту содержимого

- Выполнить анализ структуры файла `static/css/style.css` без его изменения.
- Создать файл `.planning/phases/09-css-refactor/CSS-MAP.md` с картой содержимого, включающей:
  - примерный объём строк для базовых стилей (reset, variables, typography),
  - перечень выделяемых компонентных блоков (кнопки, формы, карточки, навигация),
  - список страниц/разделов с уникальными стилями,
  - наличие и описание очевидных дубликатов.
- Зафиксировать выводы в формате, удобном для последующего рефакторинга.

**Проверка:** файл `CSS-MAP.md` создан и содержит все четыре требуемых раздела анализа.

---

## Verification Checklist

- [x] `static/css/style.css` не изменялся в рамках выполнения плана.
- [x] Создан файл `.planning/phases/09-css-refactor/CSS-MAP.md`.
- [x] В `CSS-MAP.md` есть оценка объёма базовых стилей (reset, variables, typography).
- [x] В `CSS-MAP.md` перечислены компонентные блоки (кнопки, формы, карточки, навигация).
- [x] В `CSS-MAP.md` отмечены страницы/разделы с уникальными стилями.
- [x] В `CSS-MAP.md` зафиксированы очевидные дубликаты (если обнаружены).

---

## Notes

- Ограничение Phase 9 (план 09-01): никаких изменений в `static/css/style.css`; только анализ и документирование.

---

## Completion

- [x] По завершении плана обновить `STATE.md` с отметкой о выполнении 09-01.
- Следующий шаг: план декомпозиции CSS и поэтапный перенос в `base/components/pages`.
