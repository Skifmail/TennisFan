# Plan 09-02: Phase 9 — Вынесение base-блоков CSS

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Вынести `variables`, `reset` и `typography` из `static/css/style.css` в `static/css/base/` и подключить через `@import`, не меняя селекторы.

**Источники:** `.planning/phases/09-css-refactor/09-01-PLAN.md`, `.planning/phases/09-css-refactor/CSS-MAP.md`, `static/css/style.css`.  
**Зависимости:** 09-01 выполнен.

---

## Prerequisites

- Сохранить селекторы без изменений.
- Переносить только целевые диапазоны:
  - `variables`: `47-116` и `688-692`,
  - `reset`: `139-245`,
  - `typography`: `7-45` и `118-137`.
- Подключение выполнить через `@import` в `static/css/style.css`.

---

## Task 1: Создать base-файлы

- Создать `static/css/base/typography.css` и перенести:
  - `@font-face` (`7-45`),
  - блок заголовков и навигации (`118-137`).
- Создать `static/css/base/variables.css` и перенести:
  - основной `:root` (`47-116`),
  - loading-переменные `:root` (`688-692`).
- Создать `static/css/base/reset.css` и перенести reset-блок (`139-245`).

**Проверка:** все три файла созданы, селекторы и значения в перенесённых блоках не изменены.

---

## Task 2: Подключить base-файлы в style.css

- В `static/css/style.css` удалить перенесённые блоки.
- Добавить в начало файла `@import` для:
  - `./base/typography.css`,
  - `./base/variables.css`,
  - `./base/reset.css`.
- Сохранить остальной файл без изменений.

**Проверка:** `style.css` содержит `@import`; перенесённые блоки в нём отсутствуют.

---

## Verification Checklist

- [x] Создан `static/css/base/typography.css` с блоками `7-45` и `118-137`.
- [x] Создан `static/css/base/variables.css` с блоками `47-116` и `688-692`.
- [x] Создан `static/css/base/reset.css` с блоком `139-245`.
- [x] В `static/css/style.css` добавлены `@import` на base-файлы.
- [x] В `static/css/style.css` удалены перенесённые блоки.
- [x] Ни один селектор в перенесённых блоках не изменён.

---

## Completion

- [x] План 09-02 выполнен.
- Следующий шаг: переход к декомпозиции компонентных и page-specific блоков.
