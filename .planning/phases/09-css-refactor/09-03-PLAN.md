# Plan 09-03: Phase 9 — Вынесение навигации в components/nav.css

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Вынести стили навигации из `static/css/style.css` в `static/css/components/nav.css` и подключить через `@import` без изменения селекторов.

**Источники:** `.planning/phases/09-css-refactor/09-02-PLAN.md`, `static/css/style.css`.  
**Зависимости:** 09-02 выполнен.

---

## Prerequisites

- Сохранить все селекторы и значения без изменений.
- Переносить только целевые блоки:
  - блок `header/nav desktop/mobile` (диапазон из задачи: `261-586`),
  - блок `dropdown-навигация` (диапазон из задачи: `6752-6935`).
- Подключение выполнить через `@import` в `static/css/style.css`.

---

## Task 1: Создать файл компонента навигации

- Создать `static/css/components/nav.css`.
- Перенести в него:
  - секцию `Header & Navigation` (desktop/mobile),
  - секцию `Navigation Dropdown: кнопка и меню`.
- Сохранить комментарии, селекторы, media-блоки и порядок правил.

**Проверка:** `nav.css` создан и содержит оба навигационных блока без изменений селекторов.

---

## Task 2: Подключить компонент в style.css

- Добавить `@import url('./components/nav.css');` в начало `static/css/style.css`.
- Удалить перенесённые навигационные блоки из `style.css`.
- Не трогать остальные секции стилей.

**Проверка:** `style.css` подключает `components/nav.css`, перенесённые блоки отсутствуют.

---

## Verification Checklist

- [x] Создан `static/css/components/nav.css`.
- [x] В `nav.css` перенесён блок `header/nav desktop/mobile`.
- [x] В `nav.css` перенесён блок `dropdown-навигация`.
- [x] В `static/css/style.css` добавлен `@import` на `./components/nav.css`.
- [x] Из `static/css/style.css` удалены перенесённые блоки.
- [x] Ни один селектор в перенесённых блоках не изменён.

---

## Completion

- [x] План 09-03 выполнен.
- Следующий шаг: вынесение следующих компонентных блоков (`buttons`, `forms`, `cards`) в `static/css/components/`.
