# Plan 09-04: Phase 9 — Вынесение стилей кнопок в components/buttons.css

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Вынести стили кнопок из `static/css/style.css` в `static/css/components/buttons.css`, подключить через `@import` и консолидировать дубликаты селекторов без изменения свойств.

**Источники:** `.planning/phases/09-css-refactor/09-03-PLAN.md`, `static/css/style.css`, `static/css/style.css.backup`.  
**Зависимости:** 09-03 выполнен.

---

## Prerequisites

- Сохранить значения свойств без изменений.
- Сохранить селекторы без изменений.
- Перенести блоки кнопок, соответствующие заданным диапазонам:
  - `~616-680` (базовые `.btn`),
  - `~993-1092`,
  - `~1744-1829`,
  - `~6887-6958`,
  - `~9761-9865`.
- Консолидировать повторяющиеся селекторы в один блок в пределах одного scope (включая media scope).

---

## Task 1: Создать `components/buttons.css` и перенести блоки

- Создать `static/css/components/buttons.css`.
- Перенести в него целевые button-блоки из `style.css`.
- Выполнить консолидацию дубликатов:
  - объединить повторяющиеся селекторы в один блок без изменения набора свойств.

**Проверка:** `buttons.css` содержит целевые блоки и консолидированные дубликаты селекторов.

---

## Task 2: Подключить `buttons.css` и очистить `style.css`

- Добавить `@import url('./components/buttons.css');` в начало `style.css`.
- Удалить перенесённые button-блоки из `style.css`.
- Не менять остальные секции.

**Проверка:** в `style.css` есть импорт `buttons.css`, перенесённые button-блоки удалены.

---

## Verification Checklist

- [x] Создан `static/css/components/buttons.css`.
- [x] Перенесены целевые button-блоки из `style.css`.
- [x] Консолидированы повторяющиеся селекторы без изменения свойств.
- [x] В `static/css/style.css` добавлен `@import` на `./components/buttons.css`.
- [x] В `static/css/style.css` удалены перенесённые button-блоки.
- [x] Селекторы не изменены.

---

## Completion

- [x] План 09-04 выполнен.
- Следующий шаг: вынесение `forms`/`cards` блоков в `static/css/components/`.
