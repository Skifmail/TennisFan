# Plan 09-05: Phase 9 — Вынесение form-стилей в components/forms.css

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Вынести стили форм из `static/css/style.css` в `static/css/components/forms.css`, подключить через `@import` и консолидировать дубли `\.auth-form` без изменения значений свойств.

**Источники:** `.planning/phases/09-css-refactor/09-04-PLAN.md`, `static/css/style.css`, `static/css/style.css.backup`.  
**Зависимости:** 09-04 выполнен.

---

## Prerequisites

- Сохранять селекторы и значения свойств без изменений.
- Перенести целевые блоки:
  - auth/login/register формы: `~3723-3883`, `~3975-4369`,
  - формы score/filter/inputs: `~1911-1950`, `~2150-2188`, `~9475-9519`.
- Особое внимание: консолидировать дубли `\.auth-form`.

---

## Task 1: Создать `components/forms.css` и перенести целевые блоки

- Создать `static/css/components/forms.css`.
- Перенести в файл целевые form-блоки из `style.css`.
- Перенести связанные media-блоки для тех же формовых секций.

**Проверка:** `forms.css` содержит перенесённые секции auth/filter/inputs/consent.

---

## Task 2: Консолидировать дубликаты `\.auth-form`

- Объединить повторяющиеся `\.auth-form*` селекторы в один блок в пределах одинакового scope/media.
- Не изменять значения свойств; только структурно объединять.

**Проверка:** повторяющиеся `\.auth-form*` внутри одного media-scope консолидированы.

---

## Task 3: Подключить `forms.css` и очистить `style.css`

- Добавить `@import url('./components/forms.css');` в начало `style.css`.
- Удалить перенесённые form-блоки из `style.css`.

**Проверка:** `style.css` подключает `forms.css`, а перенесённые form-правила в нём отсутствуют.

---

## Verification Checklist

- [x] Создан `static/css/components/forms.css`.
- [x] Перенесены auth/login/register формы из целевых диапазонов.
- [x] Перенесены формы score/filter/inputs из целевых диапазонов.
- [x] Консолидированы дубли `\.auth-form` без изменения значений свойств.
- [x] В `style.css` добавлен `@import` на `./components/forms.css`.
- [x] Перенесённые form-правила удалены из `style.css`.

---

## Completion

- [x] План 09-05 выполнен.
- Следующий шаг: вынесение card/page-specific блоков в отдельные `components/pages` файлы.
