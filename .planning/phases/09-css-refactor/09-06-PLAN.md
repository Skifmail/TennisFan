# Plan 09-06: Phase 9 — Вынесение card-стилей в components/cards.css

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Вынести card-стили из `static/css/style.css` в `static/css/components/cards.css`, подключить через `@import` и консолидировать дубли `\.player-card` без изменения значений свойств.

**Источники:** `.planning/phases/09-css-refactor/09-05-PLAN.md`, `static/css/style.css`, `static/css/style.css.backup`.  
**Зависимости:** 09-05 выполнен.

---

## Prerequisites

- Сохранять селекторы и значения свойств без изменений.
- Перенести целевые блоки:
  - карточки тренировок/игроков: `~1100-1297`, `~5466-5658`,
  - турнирные/ценовые карточки: `~7078-7162`, `~7605-7869`, `~8142-8666`.
- Особое внимание: консолидировать дубли `\.player-card`.

---

## Task 1: Создать `components/cards.css` и перенести целевые блоки

- Создать `static/css/components/cards.css`.
- Перенести card-секции из целевых диапазонов (в актуальных смещениях текущего `style.css`).
- Сохранить media-правила и комментарии.

**Проверка:** `cards.css` содержит training/player/tournament/pricing card-блоки.

---

## Task 2: Консолидировать дубликаты `\.player-card`

- Объединить повторяющиеся `\.player-card*` в пределах одного scope/media без изменения значений.
- Не менять семантику и порядок media-scope.

**Проверка:** дубли `\.player-card` в пределах одного scope консолидированы.

---

## Task 3: Подключить `cards.css` и очистить `style.css`

- Добавить `@import url('./components/cards.css');` в начало `style.css`.
- Удалить перенесённые card-блоки из `style.css`.

**Проверка:** в `style.css` есть импорт `cards.css`, перенесённых card-правил в нём нет.

---

## Verification Checklist

- [x] Создан `static/css/components/cards.css`.
- [x] Перенесены блоки карточек тренировок/игроков.
- [x] Перенесены турнирные/ценовые card-блоки.
- [x] Консолидированы дубли `\.player-card` без изменения значений свойств.
- [x] В `style.css` добавлен `@import` на `./components/cards.css`.
- [x] Перенесённые card-правила удалены из `style.css`.

---

## Completion

- [x] План 09-06 выполнен.
- Следующий шаг: декомпозиция page-specific блоков в `static/css/pages/`.
