# Plan 09-08: Phase 9 — Вынесение auth/tournament page-specific стилей

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Вынести page-specific блоки из `static/css/style.css` в `static/css/pages/auth.css` и `static/css/pages/tournament.css` по комментариям, подключить через `@import` без изменения селекторов и свойств.

**Источники:** `.planning/phases/09-css-refactor/09-07-PLAN.md`, `static/css/style.css`.  
**Зависимости:** 09-07 выполнен.

---

## Prerequisites

- Не менять селекторы, значения свойств, media-условия и порядок правил внутри переносимых блоков.
- Переносить только целевые секции по комментариям:
  - `auth.css`:
    - `Вращающийся логотип на форме входа`
    - `Объемная монета с логотипом`
    - `Лицевая и обратная стороны монеты`
    - `Адаптивность для мобильных устройств` (в контексте auth password field)
    - `Обертка для поля пароля с иконкой глаза`
  - `tournament.css`:
    - `FAN bracket`
    - `Блоки «Составить пару» и «Добавить участника»`
    - `Карточка матча плей-офф`
    - `Список команд/участников`
    - `Мобильная адаптация страницы управления турниром`
    - `Промежуточные результаты ТВД`

---

## Task 1: Создать page-файлы и перенести целевые блоки

- Создать `static/css/pages/auth.css` и перенести auth-блоки.
- Создать `static/css/pages/tournament.css` и перенести tournament-блоки.

**Проверка:** целевые комментарии/секции присутствуют в новых файлах.

---

## Task 2: Подключить новые page-стили и очистить style.css

- Добавить импорты в `static/css/style.css`:
  - `@import url('./pages/auth.css');`
  - `@import url('./pages/tournament.css');`
- Удалить перенесённые блоки из `style.css`.

**Проверка:** целевых секций нет в `style.css`, импорты присутствуют.

---

## Verification Checklist

- [x] Создан `static/css/pages/auth.css`.
- [x] Создан `static/css/pages/tournament.css`.
- [x] В `style.css` добавлены импорты `auth.css` и `tournament.css`.
- [x] Целевые auth/tournament-блоки удалены из `style.css`.
- [x] Селекторы и свойства не изменены (только перенос структуры).

---

## Completion

- [x] План 09-08 выполнен.
- Следующий шаг: финальная ревизия оставшихся секций в `style.css` и подготовка к завершающему этапу декомпозиции.
