# Plan 09-12: Phase 9 — Дополнение forms/auth/shop/rules остатками из style.css

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Дополнить существующие CSS-файлы секциями, оставшимися в `static/css/style.css`, без изменения селекторов и свойств.

**Обязательное требование:** не менять ни один селектор или свойство при переносе.

---

## Task 1: Дополнить static/css/components/forms.css

Добавить секцию **Forms** (строка 66 в style.css):
- `.form-group`, `.form-label`, `.form-control`
- `@media (width <= 480px)` для `.form-control`
- `.form-control:focus`, `.form-control::placeholder`
- `select.form-control`

**Проверка:** базовые стили форм присутствуют в forms.css.

---

## Task 2: Дополнить static/css/pages/auth.css

Добавить секцию **Auth page** (строка 115 в style.css):
- `.auth-page`, `.auth-card`, `.auth-card-bg`
- `.auth-hero`, `.auth-form` (базовые и модификаторы)
- `.auth-toggle-btn`, `.auth-login-title`, `.auth-wrapper`

**Проверка:** стили страницы авторизации присутствуют в auth.css.

---

## Task 3: Дополнить static/css/pages/shop.css

Добавить секцию **Shop** (строка 581 в style.css):
- Секция в style.css пустая (только заголовок)
- Добавить заголовок секции в shop.css для консистентности
- Удалить пустой блок из style.css

**Проверка:** shop.css содержит заголовок секции Shop.

---

## Task 4: Дополнить static/css/pages/rules.css

Добавить секцию **Rules page** (строка 585 в style.css):
- `.rules-page`, `.container--rules`, `.rules-header`, `.page-title--rules`
- `.rules-tabs__nav`, `.rules-tabs__btn`, `.rules-tabs__btn--active`, `.rules-tabs__btn--disabled`
- `.rules-subtabs__nav`, `.rules-subtabs__btn`, `.rules-subtabs__btn--active`
- `.rules-doc__body` и все вложенные селекторы (h2, h3, h4, p, ul, li, code, hr, strong)

**Проверка:** стили страницы правил присутствуют в rules.css.

---

## Task 5: Удалить перенесённые секции из style.css

Удалить из `static/css/style.css`:
1. Секцию Forms (строки 65–114)
2. Секцию Auth page (строки 115–254)
3. Секцию Shop (строки 581–583)
4. Секцию Rules page (строки 584–539)

**Проверка:** в style.css отсутствуют указанные секции.

---

## Verification Checklist

- [x] forms.css дополнен секцией Forms
- [x] auth.css дополнен секцией Auth page
- [x] shop.css дополнен секцией Shop (заголовок)
- [x] rules.css дополнен секцией Rules page
- [x] Перенесённые секции удалены из style.css
- [x] Селекторы и свойства не изменялись

---

## Completion

- [x] План 09-12 выполнен.
