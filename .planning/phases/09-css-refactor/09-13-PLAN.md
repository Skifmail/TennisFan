# Plan 09-13: Phase 9 — Вынос profile/club-header/pricing, удаление legacy

**Phase:** 9 (CSS рефакторинг)  
**Цель:** Вынести секции из `static/css/style.css` в profile.css, создать club-header.css и pricing.css, удалить legacy-секцию. Подключить новые файлы через @import.

**Обязательное требование:** не менять ни один селектор или свойство при переносе.

---

## Task 1: Дополнить static/css/pages/profile.css

Добавить три секции из style.css:

1. **Profile Matches Filters** (`.matches-filter-*`):
   - `.matches-filter-form`, `.matches-filter-bar`, `.matches-filter-group`
   - `.matches-filter-label`, `.matches-filter-select`, `.matches-filter-reset`
   - `.matches-quick-filters`, `.matches-quick-btn`, `.matches-count`

2. **Бейджи чемпионов** (`.profile-championships`, `.championship-badge`):
   - `.profile-championships`, `.profile-championships__title`, `.profile-championships__badges`
   - `.championship-badge` и `:hover`

3. **Оценки соперников** (`.player-skills-block` и все вложенные):
   - `.player-skills-block` и все `__*` модификаторы
   - Включая `@media (width <= 768px)` и `@media (width <= 600px)` для этих блоков

**Проверка:** все три секции присутствуют в profile.css.

---

## Task 2: Создать static/css/components/club-header.css

Создать новый файл с шапкой и секцией **Club header bar**:
- `.club-header-bar` и все вложенные (`.club-header-bar__*`)

**Проверка:** club-header.css создан, содержит полную секцию.

---

## Task 3: Создать static/css/pages/pricing.css

Создать новый файл с шапкой и:
1. Секция **Pricing**: `.pricing-intro`, `.pricing-faq` и все вложенные
2. `@keyframes coin-rotate` (используется в auth.css и nav.css — pricing.css должен загружаться рано)

Включить `@media (width <= 768px)` для `.pricing-grid`, `.pricing-card`, `.pricing-card__features`.

**Проверка:** pricing.css создан, содержит pricing-секцию и keyframes.

---

## Task 4: Удалить legacy и подключить импорты

1. **Удалить** из style.css секцию «Старые стили для обратной совместимости»:
   - `.feedback-form .form-group`, `.feedback-success`
   - `.feedback-thread`, `.feedback-thread__meta`, `.feedback-thread__message`, `.feedback-thread__reply`, `.feedback-thread__reply-date`

2. **Добавить** в style.css:
   - `@import url('./components/club-header.css');`
   - `@import url('./pages/pricing.css');`

**Важно:** pricing.css должен загружаться до auth.css и nav.css (они используют `coin-rotate`).

**Проверка:** legacy удалена, импорты добавлены.

---

## Verification Checklist

- [x] profile.css дополнен тремя секциями
- [x] club-header.css создан
- [x] pricing.css создан (pricing + coin-rotate)
- [x] Legacy-секция удалена из style.css
- [x] @import для club-header.css и pricing.css добавлены
- [x] Селекторы и свойства не изменялись

---

## Completion

- [x] План 09-13 выполнен.
